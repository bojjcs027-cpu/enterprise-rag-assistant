# Secrets — never commit
.env

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Runtime artifacts (regenerated by the app)
data/vectorstore/
data/auth.db
data/chat_history.db
data/semantic_cache.pkl
data/eval_history.json

# OS / editor noise
desktop.ini
Thumbs.db
.DS_Store

# Claude Code session state (keep only the launch config)
.claude/*
!.claude/launch.json
