apiVersion: 1

providers:
  - name: rag-dashboards
    folder: RAG Assistant
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
